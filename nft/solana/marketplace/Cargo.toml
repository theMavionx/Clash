[package]
name = "clash_solana_marketplace"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
mpl-core = "0.12.0"
solana-program = "3.0.0"
solana-system-interface = { version = "2.0.0", features = ["bincode"] }
spl-token = { version = "9.0.0", features = ["no-entrypoint"] }
