export const GOLD_REWARD_PANEL_TOAST_STYLE = Object.freeze({
  position: 'absolute',
  left: 'auto',
  right: 12,
  bottom: 34,
  width: 'min(420px, calc(100% - 24px))',
  maxWidth: 420,
  transform: 'none',
  zIndex: 30,
  borderRadius: 10,
  padding: '10px 12px',
});
